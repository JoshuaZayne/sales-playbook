# Per-repo Claude memory

This folder is for memories scoped to **this repo only**.

For shared memory across all repos, see the global pool at:
`C:\Users\ohjos\.claude\projects\C--Users-ohjos\memory\` (auto-loaded
into every Claude session via a junction at
`C:\Users\ohjos\.claude\projects\<sanitized-cwd>\memory`).

Memory types and naming follow the global convention:
`user_*.md`, `feedback_*.md`, `project_*.md`, `reference_*.md`.
Index entries should also be added to `MEMORY.md` here so the per-repo
memories are discoverable.

Created: 2026-05-01_030216 (by automated A+B memory-folder rollout).
